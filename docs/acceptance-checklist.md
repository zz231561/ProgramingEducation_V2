# 驗收清單（真人操作項目）

> 只列**必須由使用者親自操作**才能確認的項目；自動化測試涵蓋的不列入。
> 完成一項就把 `[ ]` 改成 `[x]` 並註明日期；整段完成後回寫 roadmap 對應 sub-task。
> **最後全面稽核：2026-08-06**（移除已作廢項目、依操作動線重編）

---

## 0. 開始前必做

- [ ] **重新部署 web 與 backend**。截至 2026-08-06 尚有多批未部署的變更：
  - backend：SSE 對話串流、課程全解鎖 migration `u7d8e9f0a1b2`（Zeabur 啟動時自動跑 `alembic upgrade head`）
  - web：互動終端、程式碼補全、教材出處移除、per-file 歷史等
  - ⚠ `NEXT_PUBLIC_*` 是**建置期**烘入，web 一定要重新部署才生效

### 額度限制

| 資源 | 上限 | 說明 |
|------|------|------|
| 程式執行 | **無上限** | 2026-08-06 起自建 runner（B 機 `43.133.7.93`），Judge0 50 次/天已解除 |
| LLM | **60 次/人/天** | Coddy 對話、出題、編譯錯誤主動說明共用 |
| Google OAuth | 100 人 | 測試模式；擴大前需自訂網域 |

> 只有 LLM 有額度壓力。**第 4 段（Coddy）與第 5 段（QUIZ）合計約用 15–25 次**，建議分兩天，或當天不要重複測試同一項。

---

## 1. Workspace — 執行與終端

- [x] **1-1 互動終端基本流程** — 2026-08-06 通過（含 `cin` 的程式會停下來等輸入）
- [ ] **1-2 終端視覺**：黑底 `#010409`、JetBrains Mono、游標藍色；狀態列依序顯示「編譯中」→「互動中 — 直接在下方輸入」
- [ ] **1-3 結束後收合**：程式跑完 → 終端收回成結果卡片 → 狀態圖示正確、可「詢問 Coddy」、header 的「歷史」看得到這次執行
- [ ] **1-4 執行參數**：貼下方 argv 程式 → 出現**單行**「執行參數」輸入 → 填 `hello world` → 輸出 `3 hello world`
  ```cpp
  #include <iostream>
  int main(int argc, char* argv[]) {
      std::cout << argc;
      for (int i = 1; i < argc; i++) std::cout << " " << argv[i];
      return 0;
  }
  ```
  ⚠ **不使用 argv 的程式不該看到這一列**（改前是永遠顯示）
- [ ] **1-5 逾時說明**：跑 `int main(){ while(1){} }` → 結束後 Coddy 應說**逾時**（不是執行期錯誤）
- [ ] **1-6 編譯錯誤主動說明**：
  - 貼 `#include <QApplication>` → Coddy **秒回**平台限制說明（機械判定，不花 LLM 額度）
  - 寫個漏分號 → Coddy 引導但**不給改好的程式碼**（這次會用 1 次 LLM）
  - 同一錯誤再跑一次 → **不重複發言**
- [ ] **1-7 執行歷史各檔獨立**：A 檔跑 2 次 → 另存 B 檔 → **歷史清空** → 切回 A 檔 → **A 的 2 筆還在**
- [ ] **1-8 切檔中止執行**：程式正在終端等輸入時切換檔案 → session 中止、畫布清空
- [ ] **1-9 複製回饋**：Output「複製輸出」→ 出現綠色「已複製」
- [ ] **1-10 收合聊天不遺失**：與 Coddy 對話 → 收合 → 展開 → **訊息還在**；收合狀態下跑程式 → 展開後看得到結果
- [ ] **1-11 平板版面**：視窗縮到約 800px 寬 → 聊天鈕 → **底部滑出**面板

> 若終端完全不出現、直接跳結果卡片 → 是退回批次執行了。查 `docs/deployment.md` §E 疑難排解第一項（**backend 是否已重啟**）。

## 2. Workspace — 編輯器

- [ ] **2-1 補全可用**：打 `vec` → 跳出 `vector`、右側有繁中說明 → **Tab** 或 **Enter** 插入；**Esc** 關閉不插入
- [ ] **2-2 補全不擋縮排**：沒有候選視窗時按 Tab → 正常縮排 4 空格
- [ ] **2-3 自己的變數優先**：宣告 `int playerScore = 0;` → 換行打 `play` → **`playerScore` 排在最前**、標「本檔變數」
- [ ] **2-4 補全視覺**：深色 `#161B22` 底、等寬字、已輸入字元藍色標示
- [ ] **2-5 檔案操作**：`.cpp` 尾綴鎖定、點檔名可就地改名、Ctrl/Cmd+S 存檔、我的程式碼選單載入/刪除

## 3. LEARN — 課程

- [ ] **3-1 全部解鎖**：課程列表**所有單元都可點**，沒有鎖頭或「尚未解鎖」；編號與狀態圖示仍在，看得出哪些已完成
- [ ] **3-2 單元導航**：概念說明分頁有「上一／下一單元」；切到**程式實作題／觀念題時這兩顆消失**
- [ ] **3-3 教材出處已移除**：概念說明**下方不再有「影片出處」清單**
- [ ] **3-4 播放標記**：內文**看不到 `[00:15]` 這種戳記**，改為段落結尾的淡灰 `▸ 0:15`；滑過變藍、**點下去影片跳到該秒**
- [ ] **3-5 程式碼區塊未被破壞**：概念說明若含程式碼區塊，內容不應被改動（特別是含 `[` `]` 的陣列語法）
- [ ] **3-6 觀念題秒開**：觀念題分頁不呼叫 LLM，應立即顯示題目
- [ ] **3-7 反思寬容化**：程式實作題 → 填反思 → 追問**可跳過**、送出後**看不到分數**
- [ ] **3-8 Coddy 反思開場**：承 3-7 進 Workspace → Coddy 自動開場（肯定亮點 + 邀請提問）、左側自動開反思計畫、檔名為「章節 程式實作題.cpp」
- [ ] **3-9 章節 41 內容正確**：教材寫的是 `extern`（不是 `external`）

## 4. Coddy 對話 <sub>（約用 5–8 次 LLM）</sub>

- [ ] **4-1 分階段進度**：提問 → 等待文字**依序變化**「分析你的程式碼…」→「查找相關教材…」→「整理回答…」，下方三段進度條逐段亮起。**不該從頭到尾只有「Coddy思考中…」**
- [ ] **4-2 對話功能未退化**：回應內容、對話歷史、切換 session 都與改版前一致
- [ ] **4-3 教材出處已移除**：Coddy 回應下方**不再有「教材出處」**（資料仍在 DB，只是不呈現）
- [ ] **4-4 鷹架適切度**：弱概念給框架/拆解、強概念只點 edge case
- [ ] **4-5 不直接給答案**：反覆追問也不應吐出完整可執行解答
- [ ] **4-6 錯誤處理**：若 LLM 失敗，聊天室顯示「無法取得 AI 回應，請稍後再試。」而非卡住

## 5. QUIZ 測驗 <sub>（約用 10–15 次 LLM）</sub>

- [ ] **5-1 題庫優先**：出題以「查找題庫（< 1 秒）」為主，少走「AI 生成（5–15 秒）」；同一題**不重複出現**
- [ ] **5-2 入口美化**：題型選擇卡視覺與說明清楚
- [ ] **5-3 弱項綜合測驗組**：選 10 題 → 一次生成整組、逐題作答、進度條會動
- [ ] **5-4 答錯 → 診斷**：連錯 3 題 → 出現嫌疑鏈 + 微測驗 + 補救入口，圖譜正確標紅

## 6. 知識圖譜

- [ ] **6-1 看得懂**：能看出「我在哪、哪裡弱」
- [ ] **6-2 渲染正常**：多對多箭頭正確、全覽 ↔ 章節縮放順暢、點容器/節點皆能 zoom in

## 7. 教師端（生產環境從未跑過）

- [ ] **7-1 完整動線**：切教師 → 建班 → 邀請碼 → 學生加入 → 建作業 → 學生繳交 → 教師評分
- [ ] **7-2 導航正確**：教師看到「班級 | 作業 | Workspace | Learn」，預設落地班級管理
- [ ] **7-3 題庫檢視**：單元頁有「題庫（教師）」分頁，解答預設隱藏可切換
- [ ] **7-4 邀請碼複製**：出現綠色「已複製」

## 8. 安全與設定

- [ ] **8-1 DEV 模式在生產關閉**：非白名單帳號看不到 Settings 的開發者工具區塊
- [ ] **8-2 幽靈解鎖已移除**：開發者工具**不應再有「幽靈解鎖」卡片**（其餘五張卡仍在）
- [ ] **8-3 降級保護**（無法主動測試，知道即可）：runner 若不可用會自動退回批次執行，學生無感

## 9. 持續回饋（例行，非一次性）

- [ ] **9-1 教材品質**：用到哪個單元覺得內容不對，記下編號 → 走 6-4b 局部重跑
- [ ] **9-2 教材健檢**：`cd backend && .venv/bin/python -m scripts.verify_code_snippets`
  - 每日 20 支的限制**已可解除**（Judge0 額度不再是瓶頸），待 R6 調整；跑出 `Compile Error` 就是下一個 `external` 等級的缺陷

---

## 已作廢項目（保留說明避免重複詢問）

| 原項目 | 作廢原因 |
|---|---|
| A12 標準輸入「輸入」列自動展開 | 7-R 互動終端上線後 stdin 預填 UI 移除；改由 1-1 涵蓋 |
| B7 / 6-2c citation 跳轉 | 2026-08-06 使用者已私下驗收通過；跳轉機制由 3-4 沿用 |
| 6-2d 範例程式卡片 | U2g 已移除該 tab |
| 6-2e 摘要 tab 三狀態 | U2b 已移除該 tab |
| Coddy 引用可核對（K4e 第三層） | 7-U3 依使用者決策移除教材出處 UI |
