# 驗收清單（真人操作項目）

> 只列**必須由使用者親自操作**才能確認的項目；自動化測試涵蓋的不列入。
> 完成一項就把 `[ ]` 改成 `[x]` 並註明日期；全段完成後回寫 roadmap 對應 sub-task。
> **最後更新：2026-08-05**

## 額度限制（安排驗收時先看這裡）

| 資源 | 上限 | 影響 |
|------|------|------|
| Judge0（RapidAPI 免費） | **50 次/天** | 所有「執行程式」共用；教材健檢每天也吃 20 次 |
| LLM 每人每日 | **60 次** | Coddy 對話、出題、編譯錯誤主動說明共用 |
| Google OAuth 測試模式 | **100 人** | `zeabur.app` 是公共後綴無法送審；擴大前需買自訂網域 |

---

## A. 2026-08-05 修訂批（Workspace / UI）

- [x] A1–A8（.cpp 鎖定 / 就地改名 / 輸出歷史保留 / 滾動條 / 對話歷史選單溢出 / 側欄錯誤訊息）— **2026-08-05 通過**
- [ ] **A9 執行歷史按鈕**：跑 2–3 次程式 → Output header 點「歷史」→ 列表正確 → 點舊的那筆會展開並捲到畫面內
- [ ] **A10 複製提示**：Output「複製輸出」與教師端「複製邀請碼」都要出現綠色「已複製」
- [ ] **A11 編譯錯誤主動說明**：貼 Qt 程式跑 → Coddy 秒回平台限制說明（無 LLM 延遲）；寫個漏分號跑 → Coddy 引導但**不給改好的程式碼**；同一錯誤再跑一次 → **不重複發言**
- [ ] **A12 標準輸入**：含 `cin` 的程式 → Output 上方「輸入」列自動展開並顯示「程式在等待輸入」→ 填值 → Run → 輸出正確
- [ ] **A13 執行參數**：`main(int argc, char* argv[])` → 出現「執行參數」欄 → 填 `hello world` → 輸出 `argc=3`
- [ ] **A14 逾時說明**：`while(1){}` → Coddy 主動解釋逾時（秒回）
- [ ] **A15 收合聊天**：對話 → 收合 → 展開 → **訊息還在**；收合狀態下跑程式 → 展開後看得到結果卡片
- [ ] **A16 平板**：視窗縮到 768–1023px → 點聊天鈕 → 底部滑出面板
- [ ] **A18 章節 41**：Learn v41 教材寫的是 `extern`（不是 `external`）；觀念題 4 題 + 程式實作題 1 題

## B. 從未驗過的學生端功能（2026-07-06 ~ 07-16 三批殘留）

- [ ] **B1 反思寬容化（U2h）**：Learn 開程式實作題 → 填反思 → 追問**可跳過**、送出後**看不到分數**
- [ ] **B2 Coddy 反思開場（U2i）**：承 B1 進 Workspace → Coddy 自動開場（肯定亮點 + 邀請提問）、左側自動開反思計畫、檔名為「章節 程式實作題.cpp」
- [ ] **B3 QUIZ 入口美化（U2a）**：題型選擇卡視覺與說明清楚
- [ ] **B4 弱項綜合測驗組（6-3d）**：選 10 題 → 一次生成整組、逐題作答、進度條會動
- [ ] **B5 題庫優先（U2d）**：出題以「查找題庫（<1 秒）」為主，少走「AI 生成（5–15 秒）」；同一題**不重複出現**
- [ ] **B6 LEARN 觀念題整組作答（6-3c）**：不呼叫 LLM，應秒開
- [ ] **B7 教材 citation 跳轉（6-2c）**：概念說明的時間戳點下去 → 影片跳到該秒（tech-debt 掛最久）

## C. Coddy 與知識圖譜真人驗收

- [ ] **C1 引用可核對（K4e）**：展開「教材出處」看 transcript 原文，確認 Coddy 講的內容與原文一致
- [ ] **C2 RAG 命中率（K4d）**：問 5 個課程相關問題，數有幾個附教材連結 → 太少則調 `RAG_MIN_SCORE`（現 0.40）
- [ ] **C3 鷹架適切度（K4d）**：弱概念給框架/拆解、強概念只點 edge case
- [ ] **C4 答錯 → 診斷（K3e）**：連錯 3 題 → 出現嫌疑鏈 + 微測驗 + 補救入口，圖譜正確標紅
- [ ] **C5 圖譜看得懂（K5d + K1d）**：能看出「我在哪、哪裡弱」；多對多箭頭渲染正常、全覽↔章節縮放順暢

## D. 生產環境端到端（Phase 7）

- [ ] **D1 Golden path（7-1b）**：登入 → 寫碼 → 執行 → Coddy → RAG → 出題作答
- [ ] **D2 教師端（7-1c）**：切教師 → 建班 → 邀請碼 → 學生加入 → 建作業 → 學生繳交 → 教師評分。**程式碼 2026-07-12 本機驗過，生產從未跑過**
- [ ] **D3 DEV 模式在生產正確關閉**：非白名單帳號看不到 Settings 開發者區塊

## E. 持續回饋（不是一次性）

- [ ] **E1 教材品質**：用到哪個單元覺得內容不對，記下編號 → 走 6-4b 局部重跑
- [ ] **E2 每日教材健檢**：`cd backend && .venv/bin/python -m scripts.verify_code_snippets`（每天 20 支，約 7 天一輪）。session 開場會提醒；跑出 `Compile Error` 就是下一個 `external` 等級的缺陷
